import { View } from 'react-native';
import { ThemedText } from '@/components/ThemedText';

const DriverNotification = () => {
    return (
        <View>
            <ThemedText>DriverNotification</ThemedText>
        </View>
    );
}

export default DriverNotification;
