import { View } from 'react-native';
import { ThemedText } from '@/components/ThemedText';

const DriverOnboarding = () => {
    return (
        <View>
            <ThemedText>DriverOnboarding</ThemedText>
        </View>
    );
}

export default DriverOnboarding;
