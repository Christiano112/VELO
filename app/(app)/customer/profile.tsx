import { View } from 'react-native';
import { ThemedText } from '@/components/ThemedText';

const CustomerProfile = () => {
    return (
        <View>
            <ThemedText>CustomerProfile</ThemedText>
        </View>
    );
}

export default CustomerProfile;
