import { View } from 'react-native';
import { ThemedText } from '@/components/ThemedText';

const CustomerNotification = () => {
    return (
        <View>
            <ThemedText>CustomerNotification</ThemedText>
        </View>
    );
}

export default CustomerNotification;
