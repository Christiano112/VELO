import { View } from 'react-native';
import { ThemedText } from '@/components/ThemedText';

const ResetPassword = () => {
    return (
        <View>
            <ThemedText>ResetPassword</ThemedText>
        </View>
    );
}

export default ResetPassword;
