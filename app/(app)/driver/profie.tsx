import { View } from 'react-native';
import { ThemedText } from '@/components/ThemedText';

const DriverProfile = () => {
    return (
        <View>
            <ThemedText>DriverProfile</ThemedText>
        </View>
    );
}

export default DriverProfile;
