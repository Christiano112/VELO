import { View } from 'react-native';
import { ThemedText } from '@/components/ThemedText';

const DriverEarnings = () => {
    return (
        <View>
            <ThemedText>DriverEarnings</ThemedText>
        </View>
    );
}

export default DriverEarnings;
